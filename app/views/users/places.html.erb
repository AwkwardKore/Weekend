<% if logged_in? %>
<p id="notice"><%= notice %></p>

<div class="row">
  <%= link_to "<button class='btn btn-primary btn-raised'>Volver a lugares</button></p>".html_safe, places_path %>
</div>
<h1 style="color: white;">Tus favoritos son: </h1>


<% @places.each do |place| %>
  <div>
      <div class="col-md-2">
        <div class="jumbotron" style="text-align: center;">
          <%= link_to image_tag(place.image, class: "mini-image", :border => 0), place %>
          <p><%= place.name %></p>
        </div>
      </div>
  </div>
  <% end %>
<% end %>
